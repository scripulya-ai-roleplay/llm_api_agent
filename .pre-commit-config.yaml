repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Update 'rev' to the version of Ruff you are using
    rev: v0.15.13
    hooks:
      # Run the linter
      - id: ruff
        args: [ --fix ]
      # Run the formatter
      - id: ruff-format
